
export default class ThreeUtil {
  
  /**
   * Given a React ref, return the pixel coordinates of the center of the element
   * @param {*} ref 
   */
  static getRefPixelCenter(ref) {
    // console.log(ref);
  }

}